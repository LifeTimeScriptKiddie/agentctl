# Handoff: closed — 2026-09-22 / 2026-09-23

## Status

**Merged and live on `main`** at `~/code/agentctl/dev` (tip includes team KB + `agentctl setup`).  
Worktree `dev-arch-review` is **gone**. Do **not** rebase or re-merge arch-review-fixes.

Installed CLI: Homebrew `agentctl` → this checkout (`0.2.0`).

## Shipped on main

1. Architecture P0–P4 — `docs/ARCHITECTURE-REVIEW-2026-09-22.md`
2. Security S1–S8 — `docs/SECURITY-REVIEW-2026-09-22.md` (accepted residuals documented there)
3. Team shared knowledge — `docs/TEAM-SHARED-KNOWLEDGE.md` (KB / findings / evidence; SQLite `user_version=5`; Postgres `005_findings_evidence.sql`)
4. `agentctl setup` — preferences at `$AGENTCTL_HOME/preferences.yaml`
5. Skill entry — `~/code/skillz/ai-agents/agentctl` (Cursor/Claude symlinks; Pi keeps `/agentctl`)

## Live ops proof (this host)

- Gatekeeper: `agentctl memory serve` on `127.0.0.1:8741` with reviewer group `reviewers`
- Workspace `team-ops-proof`: finding `RT-2026-001`, evidence pointer, propose→accept across two tokens
- Note: `~/code/atoz/projects/agentctl/gatekeeper-ops-proof-2026-09-22.md`
- Postgres: migration **005** listed; **store adapter still sqlite-only** until a VM wires `AGENTCTL_MEMORY_DATABASE_URL`

## Next session — start here

High leverage left (do in order if continuing the backlog):

5. Router catalog → presets (arch F12 rest) — own reviewed PR  
6. Health-probe TTL cache  
7. `MemoryStorePort` + leftover import cycles  
8. SessionGraph propose→accept (only after gatekeeper stays boring)

Daily use: load skill `agentctl` or Pi `/agentctl`; prefer `delegate --dry-route` before spend.

## Anti-ghosts

| Stale claim | Reality |
| --- | --- |
| Branch `arch-review-fixes` unmerged | Merged into `main` |
| Worktree `dev-arch-review` present | Removed |
| Test with branch `dist` only | Use PATH `agentctl` / `~/code/agentctl/dev/dist` |
| Postgres 005 applied on laptop | Dry-run only; sqlite serve is the live plane here |
