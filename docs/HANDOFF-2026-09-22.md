# Handoff: arch-review-fixes (stopped 2026-09-22)

## Where things stand

- **Branch:** `arch-review-fixes`, 23 commits on top of `main@559afa4`. Tip: `45eee77`. Not merged, not pushed.
- **Worktree:** `~/code/agentctl/dev-arch-review` (node_modules is a symlink to `../dev/node_modules`).
- **Checks at the tip:**
  - `npx tsc --noEmit`: clean.
  - `npx vitest run`: 781 passed / 3 skipped. It also passes with `FORCE_COLOR=3`.
  - `npm run build`: passes.
  - `node scripts/qa-gatekeeper-smoke.mjs`: 13/13.

## Done

1. **Architecture review (P0–P4):** `docs/ARCHITECTURE-REVIEW-2026-09-22.md`.
2. **Security review and fixes (S1–S8):** `docs/SECURITY-REVIEW-2026-09-22.md`. It records every finding, three Opus 5.5 verification passes, and the accepted residual risks table. No High or Medium findings remain open.

## Not started

- **Team shared knowledge work** (tiered KB, findings tracker, evidence pointers). It was discussed on 2026-09-22 but paused before any code was written. Nothing for it exists on this branch.
- **Before-merge checklist.** Nothing on it has been done:
  1. Rebase onto current `main`. Another session commits to `main` in `~/code/agentctl/dev`, so check that tree is clean before merging there.
  2. Migrate the memory server:
     - Run `agentctl memory serve token add --user …` for each person, and give each client `AGENTCTL_GATEWAY_TOKEN`.
     - On the server, set `AGENTCTL_MEMORY_REVIEWER_GROUPS` and `AGENTCTL_USER_ID`.
     - Use `http://127.0.0.1:<port>` as the gateway URL, not `localhost`.
     - Re-set existing checkpoints with `--groups`.
     - Run `agentctl memory postgres migrate` if the server uses Postgres.
  3. Review and trust repo-local `agents.yaml` files with `agentctl config trust`. Until you do, they are ignored.
  4. Rebuild after merging. The installed `agentctl` links to `~/code/agentctl/dev/dist`, which is the old `main` build.
  5. Clean up with `git worktree remove ../dev-arch-review`. That also removes the temporary `cursor_write` lane config kept in the worktree's git dir.

## Notes for whoever resumes

- **Cursor's Opus quota is exhausted until 2026-09-27.** S8 was implemented directly by Claude.
- **Test with the branch build** (`node dist/cli.js`), not the installed `agentctl`.
- **On macOS, don't use `timeout`.** It isn't installed, so commands wrapped in it exit 127 without running. Close stdin (`</dev/null`) when running `codex exec` by hand.
