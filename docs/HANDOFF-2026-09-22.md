# Handoff: arch-review-fixes (updated 2026-09-22 evening)

## Where things stand

- **Branch:** `arch-review-fixes`. Tip includes team shared knowledge (tiered KB / findings / evidence). Not merged, not pushed.
- **Worktree:** `~/code/agentctl/dev-arch-review` (node_modules is a symlink to `../dev/node_modules`).

## Done

1. **Architecture review (P0–P4):** `docs/ARCHITECTURE-REVIEW-2026-09-22.md`.
2. **Security review and fixes (S1–S8):** `docs/SECURITY-REVIEW-2026-09-22.md`.
3. **Team shared knowledge (tiered model):**
   - Markdown KB scaffold: `agentctl memory kb init` → `$AGENTCTL_HOME/kb/`
   - Findings tracker: `agentctl memory finding …` + `/v1/finding/*`
   - Evidence pointers: `agentctl memory evidence …` + `/v1/evidence/*`
   - Kinds: `playbook`, `lesson` (+ existing `technique`)
   - Docs: `docs/TEAM-SHARED-KNOWLEDGE.md`, README tier section
   - Schema: SQLite `user_version=5`, Postgres `005_findings_evidence.sql`

## Before-merge checklist

1. Rebase onto current `main` (check `~/code/agentctl/dev` is clean).
2. Migrate the memory server (tokens, reviewer groups, `127.0.0.1`, Postgres migrate including **005**).
3. `agentctl config trust` for repo-local `agents.yaml` files.
4. Rebuild after merging (`~/code/agentctl/dev/dist`).
5. `git worktree remove ../dev-arch-review`.

## Notes

- Test with the branch build (`node dist/cli.js`), not the installed `agentctl`.
- Secrets stay out of agentctl — pointers and Markdown only.
