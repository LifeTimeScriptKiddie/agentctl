# agentctl flow architecture

Mermaid sources in this folder. Preview in any Mermaid renderer (Cursor, GitHub, Obsidian).

```bash
npx @mermaid-js/mermaid-cli -i flow-architecture.mmd -o flow-architecture.svg
```

## Files

| File | Diagram |
|------|---------|
| `flow-architecture.mmd` | Full system stack + data stores |
| `flow-ask.mmd` | `ask` / `chat` sequence |
| `flow-orchestrate.mmd` | `orchestrate` step loop |
| `flow-run.mmd` | `run` improvement loop |
