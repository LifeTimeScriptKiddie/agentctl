Write a short markdown document about `agentctl`.

It must include:
- a `## Overview` section explaining, in 2–3 sentences, that agentctl is one CLI to talk to multiple local AI agents (claude, codex, cursor, agy, comet) while preserving each agent's login session, plus a controlled improvement loop;
- a `## Examples` section with at least two concrete command-line usage examples.

Keep it under 200 words. Output only the markdown.
