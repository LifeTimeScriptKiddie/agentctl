name: pi
family: subprocess
transport: subprocess
parse: text
optional: true
# Pi's -p switch selects non-interactive print mode; the prompt itself is the
# final argv value. Keep sessions off here because agentctl owns orchestration
# state, and expose only read-only context tools in this lane.
promptDelivery: arg
commandTemplate:
  - pi
  - -p
  - --mode
  - text
  - --no-session
  - --tools
  - read,grep,find,ls
  # `pi --help` (0.85.1): "--  End option parsing; treat remaining arguments as
  # messages/files", so a prompt starting with '-' stays a message. buildInvocation
  # inserts --model and other appended flags before this `--`.
  - --
  - "{prompt}"
# Example provider-qualified model roster. Configure these for your provider account.
models:
  flag: --model
  default: openai-codex/gpt-5.6-luna
  options:
    - openai-codex/gpt-5.3-codex-spark
    - openai-codex/gpt-5.6-luna
    - openai-codex/gpt-5.6-sol
healthProbe: [which, pi]
capabilities:
  canReadFiles: true
  canWriteFiles: false
  canRunShell: false
  canAccessNetwork: false
  canUseBrowser: false
  canModifyRepo: false
  canPublish: false
